<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Theo dÃµi váº­n Ä‘Æ¡n | SKYNET Store</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />
  <link href="assets/css/common.css" rel="stylesheet" />
  <style>
    /* Custom Timeline & Tracking Styles */
    .sk-carrier-pill {
      padding: 8px 16px;
      background: var(--surface-soft);
      border: 1px solid var(--line);
      border-radius: 99px;
      font-size: 13px;
      font-weight: 600;
      color: var(--muted);
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: all 0.2s;
      user-select: none;
    }
    .sk-carrier-pill.active {
      background: #ee4d2d;
      color: #fff;
      border-color: #ee4d2d;
    }
    .sk-timeline {
      padding-left: 14px;
      margin-top: 10px;
    }
    .sk-timeline-row {
      position: relative;
      padding: 0 0 24px 32px;
    }
    .sk-timeline-row::before {
      position: absolute;
      top: 12px;
      bottom: -16px;
      left: 11px;
      width: 2px;
      background: var(--line);
      content: "";
    }
    .sk-timeline-row:last-child {
      padding-bottom: 0;
    }
    .sk-timeline-row:last-child::before {
      display: none;
    }
    .sk-timeline-event {
      position: relative;
      transition: all 0.2s ease;
    }
    .sk-timeline-icon {
      position: absolute;
      left: -34px;
      top: 0px;
      width: 26px;
      height: 26px;
      border-radius: 50%;
      background: var(--line);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 10px;
      border: 2px solid var(--surface);
      box-shadow: 0 0 0 1px var(--line);
      transition: all 0.2s ease;
      z-index: 2;
    }
    .sk-timeline-icon.icon-ready_to_pick {
      background: var(--muted);
      box-shadow: 0 0 0 1px var(--muted);
    }
    .sk-timeline-icon.icon-picking,
    .sk-timeline-icon.icon-picked,
    .sk-timeline-icon.icon-picked_to_storing,
    .sk-timeline-icon.icon-storing,
    .sk-timeline-icon.icon-delivered {
      background: var(--success);
      box-shadow: 0 0 0 1px var(--success);
    }
    .sk-timeline-icon.icon-transporting,
    .sk-timeline-icon.icon-delivering {
      background: #f59e0b;
      box-shadow: 0 0 0 1px #f59e0b;
    }
    .sk-timeline-icon.icon-return {
      background: #7c3aed;
      box-shadow: 0 0 0 1px #7c3aed;
    }
    .sk-timeline-icon.icon-cancel {
      background: var(--danger);
      box-shadow: 0 0 0 1px var(--danger);
    }
    .sk-timeline-time {
      font-size: 11px;
      color: var(--muted);
      font-weight: 600;
      display: block;
      margin-bottom: 4px;
    }
    .sk-timeline-title {
      font-size: 13px;
      font-weight: 700;
      color: var(--text);
      margin: 0 0 4px 0;
    }
    .sk-timeline-desc {
      font-size: 13px;
      color: var(--muted);
      margin: 0;
    }
    /* Style for the most recent timeline event */
    .sk-timeline-row.latest {
      padding-bottom: 28px;
    }
    .sk-timeline-row.latest .sk-timeline-event {
      background: rgba(238, 77, 45, 0.03);
      border: 1px solid rgba(238, 77, 45, 0.12);
      border-radius: 10px;
      padding: 12px 16px;
      margin-left: -12px;
      box-shadow: 0 4px 12px rgba(238, 77, 45, 0.02);
    }
    .sk-timeline-row.latest .sk-timeline-icon {
      left: -37px;
      top: 8px;
      width: 32px;
      height: 32px;
      font-size: 13px;
      background: #ee4d2d !important;
      box-shadow: 0 0 0 2px var(--surface), 0 0 12px rgba(238, 77, 45, 0.3) !important;
      border: none;
    }
    .sk-timeline-row.latest .sk-timeline-title {
      font-size: 14px;
      color: #ee4d2d;
      font-weight: 800;
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .sk-timeline-row.latest .sk-timeline-title::after {
      content: "Má»›i nháº¥t";
      font-size: 9px;
      font-weight: 800;
      color: #fff;
      background: #ee4d2d;
      padding: 1px 6px;
      border-radius: 4px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .sk-timeline-row.latest .sk-timeline-desc {
      color: var(--text);
      font-weight: 500;
      font-size: 13.5px;
    }
    .sk-timeline-row.latest::before {
      top: 24px;
      left: 11px;
    }
    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    .sk-watch-row {
      display: grid;
      grid-template-columns: 80px minmax(0, 1fr) auto;
      gap: 12px;
      align-items: center;
      padding: 12px;
      background: var(--surface-soft);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      transition: all 0.2s;
    }
    .sk-watch-row:hover {
      border-color: var(--line-strong);
    }
    .sk-watch-code {
      font-family: monospace;
      font-weight: 700;
      color: var(--text);
      overflow-wrap: anywhere;
    }
    .sk-watch-meta {
      font-size: 12px;
      color: var(--muted);
      margin-top: 4px;
      line-height: 1.5;
